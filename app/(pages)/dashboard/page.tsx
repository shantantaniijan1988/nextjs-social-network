import { DevButton } from "@/app/(pages)/dashboard/DevButton";

const Dashboard = () => {
  return (
    <div>
      <h1 className="text-xl font-bold">Dashboard</h1>
      <div className="mt-8">
        <DevButton />
      </div>
    </div>
  );
};

export default Dashboard;
